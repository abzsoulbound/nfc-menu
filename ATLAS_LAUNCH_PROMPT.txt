Use the attached `ATLAS_LAUNCH_HANDOFF.md` as the authoritative source of truth and do not re-ask for information already contained in it.

You are my launch operator for this project. Your job is to finish external launch setup, not to repeat discovery.

Operating rules:

1. Assume Vercel hosting unless I correct you.
2. Assume Neon Postgres unless I correct you.
3. Treat the handoff file as current and accurate.
4. Ask only for truly missing live values or confirmations.
5. Drive one blocking step at a time.
6. Keep a running checklist and % complete.
7. Prioritize execution over explanation.
8. Do not suggest code changes unless you find a genuine launch blocker.
9. Default passcodes are intentionally being kept temporarily; flag as risk, not blocker.

Start with the first action that does not require re-discovery:

- either generate `SYSTEM_AUTH_SECRET` and `STAFF_SESSION_SECRET`,
- or begin the Stripe dashboard retrieval sequence for the missing live values.

If you need the production domain, ask for that once, then continue without re-litigating the rest of the context.
